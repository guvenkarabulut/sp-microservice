version: '3.1'
services:

  mongo:
    image: mongo
    restart: always
    ports:
      - 27017:27017

  db:
    image: mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: 'mysql'
    ports:
      - "3306:3306"
    volumes:
      - ./data.sql:/docker-entrypoint-initdb.d/data.sql